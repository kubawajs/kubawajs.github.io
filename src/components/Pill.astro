---
interface Pill {
	url?: string;
}

const { url } = Astro.props;
---

{
	(url ? <a href={url} data-astro-prefetch><div class="pill"><slot /></div></a> : <div class="pill"><slot /></div>)
}


<style>
	.pill {
		display: flex;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--accent-text-over);
		border: 1px solid var(--accent-regular);
		background-color: var(--accent-regular);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
	}

	a div.pill:hover {
		background-color: var(--accent-dark);
		color: var(--accent-subtle-overlay);
	}
</style>
